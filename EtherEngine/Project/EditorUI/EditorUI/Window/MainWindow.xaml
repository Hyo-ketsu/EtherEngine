<ma:MetroWindow x:Class="EditorUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ma="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:av="https://github.com/Dirkster99/AvalonDock"
        xmlns:local="clr-namespace:EditorUI"
        mc:Ignorable="d"
        TitleCharacterCasing="Normal"
        Title="MainWindow" Height="450" Width="800">
   <ma:MetroWindow.DataContext>
      <local:MainVM/>
   </ma:MetroWindow.DataContext>
   
   <ma:MetroWindow.Resources>
      <!-- Menuの既定のスタイルを指定 -->
      <Style TargetType="Menu" BasedOn="{StaticResource {x:Type Menu}}"/>
      <!-- MenuItemの既定のスタイルを指定 -->
      <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}"/>
   </ma:MetroWindow.Resources>
   
   <Grid>
      <Grid.RowDefinitions>
         <RowDefinition Height="20"/>
         <RowDefinition Height="15"/>
         <RowDefinition Height="*"/>
         <RowDefinition Height="15"/>
      </Grid.RowDefinitions>

      <!-- メニュー -->
      <Menu Grid.Row="0" Name="Menu">
         <MenuItem Header="Main"/>
         <MenuItem Header="Window" ItemsSource="{Binding CreateWindow}">
            <MenuItem.ItemContainerStyle>
               <Style TargetType="MenuItem">
                  <Setter Property="Header" Value="{Binding Name}" />
                  <Setter Property="Command" Value="{Binding CreateCommnad}" />
               </Style>
            </MenuItem.ItemContainerStyle>
         </MenuItem>
         <MenuItem Header="UserWindow"/>
      </Menu>

      <!-- ウィンドウ本体 -->
      <Grid Grid.Row="1">
         <av:DockingManager Grid.Row="2" x:Name="DockManager">
            <av:LayoutRoot x:Name="DockRoot">
            </av:LayoutRoot>
         </av:DockingManager>
      </Grid>
   </Grid>
</ma:MetroWindow>
